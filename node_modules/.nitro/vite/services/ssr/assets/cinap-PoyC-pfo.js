//#region src/lib/cinap.ts
/** Art. 7º — mensalidade por categoria da congregação. */
var MENSALIDADE_POR_CATEGORIA = {
	Bronze: 40,
	Prata: 50,
	Ouro: 60
};
var CATEGORIAS = [
	"Bronze",
	"Prata",
	"Ouro"
];
var CARGOS = [
	"Pastor Presidente",
	"Pastor Auxiliar",
	"Evangelista",
	"Presbítero",
	"Diácono",
	"Obreiro"
];
function brl(valor) {
	return new Intl.NumberFormat("pt-BR", {
		style: "currency",
		currency: "BRL"
	}).format(valor);
}
function dataBR(iso) {
	const [ano, mes, dia] = iso.slice(0, 10).split("-");
	return `${dia}/${mes}/${ano}`;
}
var STATUS_LABEL = {
	pago: "Adimplente",
	pendente: "Pendente",
	atrasado: "Inadimplente"
};
function categoriaClasses(categoria) {
	if (categoria === "Ouro") return "bg-ouro text-ouro-foreground";
	if (categoria === "Prata") return "bg-prata text-prata-foreground";
	return "bg-bronze text-bronze-foreground";
}
function statusClasses(status) {
	if (status === "pago") return "bg-success/10 text-success";
	if (status === "pendente") return "bg-warning/15 text-warning-foreground";
	return "bg-destructive/10 text-destructive";
}
/** Referência do mês corrente no formato MM/AAAA. */
function referenciaAtual(hoje = /* @__PURE__ */ new Date()) {
	return `${String(hoje.getMonth() + 1).padStart(2, "0")}/${hoje.getFullYear()}`;
}
//#endregion
export { brl as a, referenciaAtual as c, STATUS_LABEL as i, statusClasses as l, CATEGORIAS as n, categoriaClasses as o, MENSALIDADE_POR_CATEGORIA as r, dataBR as s, CARGOS as t };
